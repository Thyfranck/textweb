<!DOCTYPE html>
<html lang="en">
  <head>
    <meta charset="utf-8"/>
    <meta http-equiv="X-UA-Compatible" content="IE=Edge,chrome=1"/>
    <meta name="viewport" content="width=device-width, initial-scale=1.0"/>
    <title><%= content_for?(:title) ? yield(:title) : "Course Links - Learn More" %></title>
    <%= csrf_meta_tags %>

    <!-- Le HTML5 shim, for IE6-8 support of HTML elements -->
    <!--[if lt IE 9]>
      <script src="//cdnjs.cloudflare.com/ajax/libs/html5shiv/3.6.1/html5shiv.js" type="text/javascript"></script>
    <![endif]-->

    <%= stylesheet_link_tag "application", :media => "all" %>

    <!-- Size should be 32 x 32 pixels -->
<%#= favicon_link_tag 'favicon.ico', :rel => 'shortcut icon' %>

    <%= javascript_include_tag "application" %>
  </head>
  <body>

    <%= render 'shared/header' %>


    <div class="container-fluid">
      <%= bootstrap_flash %>
      <div class="row-fluid">

        <div class="span3">
          <div class="well sidebar-nav">
            <ul class="nav nav-list">
              <li class="nav-header">Sidebar</li>
              <li class="<%= "active" if current_page?(current_user) %>">
                <%= link_to "Profile", current_user %>
              </li>
              <li class="<%= "active" if current_page?(settings_user_path(current_user)) %>">
                <%= link_to "Change Password", settings_user_path(current_user) %>
              </li>
            </ul>
          </div><!--/.well -->
        </div><!--/span-->
        <div class="span9">
          <%= yield %>
        </div>

      </div><!--/row-->
    </div> <!-- /container -->

    <%= render "shared/footer" %>
    <%= yield :js %>
  </body>
</html>