<% unless @topic.errors.any?  %>
  $("form#topic-form-of-<%= @topic.course.id %>").hide();
  $("#topic-list-of-<%= @topic.course.id %>").children("li:first").after("<%= escape_javascript(render "admin/topics/topic_tiny").html_safe %>");
<% end %>