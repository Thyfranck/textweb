<%= semantic_form_for Topic.new, :url => admin_school_course_topics_path(@school, course), :html => {:style => "display: none"} do |f| %>
  <%= f.inputs do %>
    <%= f.input :name %>
  <% end %>
  <%= f.actions :submit, :cancel %>
<% end %>